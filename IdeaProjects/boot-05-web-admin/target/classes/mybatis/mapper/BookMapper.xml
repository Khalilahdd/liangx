<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.admin.mapper.BookMapper">

    <!--    public Book getBook(xLong id); -->
    <select id="getBookById" resultType="com.atguigu.admin.bean.Book">
        select *
        from book.t_book
        where id = #{id}
    </select>

</mapper>